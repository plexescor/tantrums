// Test 1: Comment at absolute start of file
// Test 2: Identifiers starting with or containing underscores
int32 _my_var = 10;
bool _flag = true;

// Test 3: Multi-char operators directly glued to identifiers
if(a->b==c) {
    _my_var++;
}

// Test 4: String with escaped internal quotes
string msg = "Hello \"World\"!";

// Test 5: Malformed string ending at EOF (Unterminated)
string broken = "This string never ends